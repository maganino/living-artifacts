# tray

Pointer only. The canonical `tray/` spec lives in
[PMOS](https://github.com/maganino/product-management-os) at `tray/README.md` —
schema, statuses, granularity, capture and the cross-repo Obsidian dashboard.

## One addition used by this repo

Alongside `type: thread`, this repo writes **`type: artefact`** records — one per
living document, so the same Bases view can show what exists, where it stands,
and every version that has been shared. Template: `artefact-record-template.md`.
