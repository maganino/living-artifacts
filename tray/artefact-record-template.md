---
type: artefact
status: wip
area: <repo name>
topic: <thread or initiative>
created: YYYY-MM-DD
title: <document title — the stable noun, same as the artifact>
doc_id: <docId, the db path segment>
url: <living artifact URL — republish here, never fork>
source: <path to the .doc.json this is built from>
---

One line on what this document is for.

## Shared versions

Each export gets its **own** artifact URL, so the history of what was shared
with whom stays intact. Never overwrite a shared copy.

| Date | Labels | URL | Sent to |
|---|---|---|---|
| YYYY-MM-DD | `#dev` `#eng` | <url> | <who> |

## Final

When the document is done: the static HTML is committed at `<path>`, and this
record's `status` becomes `done`.
